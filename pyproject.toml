[project]
name = "easy-sdc"
version = "0.1.0"
description = ""
authors = [
    {name = "Demetrius Albuquerque",email = "demetrius.albuquerque@yahoo.com.br"}
]
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
]

[tool.poetry]
packages = [{include = "easy_sdc", from = "src"}]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
